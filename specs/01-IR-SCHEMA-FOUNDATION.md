# Domain: IR & Schema Foundation
**Priority:** P0 · **Status:** ✅ ALREADY IMPLEMENTED — Verify Only · **Dependencies:** None
**Agent Boundary:** This domain is ALREADY COMPLETE in the repo. Your job is to VERIFY correctness, add missing type exports, and ensure backward compatibility. Do NOT rewrite schemas.

---

## 1. Current State (Verified from Repo)

The file `src/translator/ir.ts` already contains a **complete IR v2.0 implementation** with:

- ✅ `SpatialAnchorSchema`, `PersonaSchema`, `RuleSchema`, `SkillSchema`, `HookSchema`
- ✅ `SettingsSchema` (Layer 6) — approval_mode, model_config, cost_controls, safety_modes
- ✅ `CommandSchema` (Layer 7) — name, description, aliases, tools_required, approval_scope
- ✅ `MCPServerSchema` (Layer 8) — name, endpoint, auth_scope, tools, risk_level, governance, tool_registry
- ✅ `ToolRegistryEntrySchema` — per-tool auth within MCP
- ✅ `IdentitySchema` — rbac_enabled, roles, agent_owner, iam_policy
- ✅ `AuditSchema` — audit_enabled, log_level, correlation_id_format, retention_days, compliance_checkpoints
- ✅ `ComplianceSchema` — frameworks, compliance_gaps, certified
- ✅ `RiskSchema` — risk_tier, risk_signals, escalation_rules
- ✅ `CrossAgentCommunicationSchema` — message_schema, shared_state_schemas, inter_agent_validation, communication_protocol
- ✅ `AgentRegistryEntrySchema` — name, owner, purpose, risk_tier, eval_status, version, capabilities, dependencies
- ✅ `AgentRegistrySchema` — agents array, registry_version, last_updated
- ✅ `RegistrySchema` — registry_name, published_version, dependencies, verified_publisher
- ✅ `OrchestrationSchema` — agent_teams, agent_chains, persistent_memory, cross_agent_communication
- ✅ `TelemetrySchema` — OpenTelemetry, Datadog, New Relic, Prometheus, CloudWatch configs
- ✅ `CostSchema` — cost_tracking, budgets, per-agent budgets, token tracking
- ✅ `MetricsSchema` — latency baselines, error rates, per-skill metrics, custom metrics
- ✅ `AlertingSchema` — policy violations, budget overrun, anomaly detection, notification channels
- ✅ `SemanticDriftSchema` — behavioral analysis, rule effectiveness, cost drift, output comparison
- ✅ `BlueprintIRSchema` — v2.0 root schema combining ALL layers
- ✅ Full type exports for all schemas

---

## 2. Verification Tasks

### Task 1.1: Schema Completeness Audit
- [ ] Verify ALL fields from original spec v2.0 are present in `src/translator/ir.ts`
- [ ] Check for any missing optional fields (e.g., `disable_model_invocation` on skills)
- [ ] Verify Zod validation rules match spec (min/max lengths, enum values, defaults)

### Task 1.2: Missing Type Exports
The following types may need explicit export (check if already exported):
- [ ] `Settings`
- [ ] `Command`
- [ ] `MCPServer`
- [ ] `ToolRegistryEntry`
- [ ] `Identity`
- [ ] `Audit`
- [ ] `Compliance`
- [ ] `Risk`
- [ ] `CrossAgentCommunication`
- [ ] `AgentRegistryEntry`
- [ ] `AgentRegistry`
- [ ] `Registry`
- [ ] `Orchestration`
- [ ] `Telemetry`
- [ ] `Cost`
- [ ] `Metrics`
- [ ] `Alerting`
- [ ] `SemanticDrift`

### Task 1.3: Backward Compatibility
- [ ] Ensure `BlueprintIRSchema` validates both v1.0 and v2.0 data
- [ ] Add `isV1()` and `isV2()` type guards if not present
- [ ] Verify existing adapters (Claude, Cursor) still compile with v2.0 IR

### Task 1.4: AGENTS.md Generator Verification
- [ ] Check `src/translator/adapters/agents-md.ts` exists and is imported by all adapters
- [ ] Verify it generates: personas, rules summary, MCP servers, compliance, risk tier
- [ ] Test output on all 10 backends

### Task 1.5: Adapter Interface Verification
- [ ] Verify all 10 adapters implement `BlueprintAdapter` interface correctly
- [ ] Check `parse()` returns `Promise<BlueprintIR>`
- [ ] Check `render()` takes `(ir: BlueprintIR, projectRoot: string)` and returns `Promise<string[]>`

---

## 3. Minor Additions (If Missing)

### 3.1 Skill Schema Enhancement
If `disable_model_invocation` is missing from `SkillSchema`:
```typescript
export const SkillSchema = z.object({
  name: z.string(),
  description: z.string(),
  when_to_use: z.string(),
  tools_required: z.array(z.string()),
  procedure: z.string(),
  disable_model_invocation: z.boolean().optional(), // ADD if missing
});
```

### 3.2 Meta Schema Enhancement
If `schema_version` is missing from `MetaSchema`:
```typescript
export const MetaSchema = z.object({
  rule_precedence: z.array(z.string()),
  conflict_resolution: z.string(),
  source_backend: z.string(),
  target_backend: z.string(),
  schema_version: z.enum(["1.0", "2.0"]).default("2.0"), // ADD if missing
});
```

---

## 4. Acceptance Criteria

- [ ] ALL schemas from original spec v2.0 are present in `src/translator/ir.ts`
- [ ] ALL types are properly exported
- [ ] `BlueprintIRSchema` validates v2.0 data correctly
- [ ] All 10 adapters compile without type errors
- [ ] `AGENTS.md` generated by all adapters
- [ ] `npm run typecheck` exits 0
- [ ] `bun test` passes (no regressions)

---

## 5. Cross-References

| Concern | File | Status |
|---------|------|--------|
| Backend adapters using IR | `02-BACKEND-EXPANSION.md` | ✅ Already implemented |
| Risk tier from detector | `03-DETECTOR-ENHANCEMENT.md` | ✅ Already implemented |
| Validator using IR schemas | `04-VALIDATOR-ENHANCEMENT.md` | ⚠️ Partial |
| Templater using IR context | `05-TEMPLATER-ENHANCEMENT.md` | ⚠️ Partial |

---

*Domain Spec: IR & Schema Foundation · VERIFY ONLY · open-blueprint v2.0*
