name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test with coverage
        run: npm run test:coverage

      - name: Build
        run: npm run build

      - name: Validate compiled output
        run: node dist/cli/index.js --version

      - name: Generate SBOM
        run: npm run sbom

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Docker image smoke test (init + verify)
        run: |
          smoke=$(mktemp -d)
          mkdir -p "$smoke/src"
          printf '{"name":"smoke","version":"1.0.0","scripts":{"test":"vitest"}}\n' > "$smoke/package.json"
          printf 'export {};\n' > "$smoke/src/index.ts"
          chmod -R 777 "$smoke"
          docker run --rm -v "$smoke":/work -w /work ghcr.io/${{ github.repository }}:${{ github.sha }} init claude
          docker run --rm -v "$smoke":/work -w /work ghcr.io/${{ github.repository }}:${{ github.sha }} verify

      - name: Docker Scout scan
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ghcr.io/${{ github.repository }}:${{ github.sha }}
          exit-code: true
          severity: HIGH,CRITICAL
        continue-on-error: false

      - name: Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release
