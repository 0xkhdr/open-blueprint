## Security Baseline

- No hardcoded secrets in source code
- Validate all inputs at system boundaries
- Handle errors explicitly — never swallow exceptions silently
- Log errors with context, never with sensitive data
